services:
  - type: web
    name: cool-toolbox-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: bash -lc "uvicorn api.app.main:app --host 0.0.0.0 --port $PORT"

